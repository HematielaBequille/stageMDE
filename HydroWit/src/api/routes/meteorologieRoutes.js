const express = require("express");
const router = express.Router();
const meteorologieController = require("../controllers/meteorologieController");

// GET - 
router.get("", meteorologieController.)