export class SearchBarService {
  submitApplication(item1: string, item2: string, item3: string) {
    console.log(
      `formulaire envoyé: item1: ${item1}, item2: ${item2}, item3: ${item3}.`
    );
  }
}
